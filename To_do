To do:

1) Home Page (LOG IN)
---------------------------------------------
     JS user_authentication
        If user logs in succesfully, take them to the editor (Check users.JSON)
        Else Returns an Invalid Login Message (Returns String)
 ---------------------------------------------
     JS user_create
        If username is unique create account (Checks users.JSON) (Writes into user.JSON)
        Else returns an Invalid Sign Up Message (Returns String)
----------------------------------------------

______________________________________________

2) Editor Page (SAVING FILES)
----------------------------------------------
     User can input code (DONE)
     Dynamically displays code (DONE)
----------------------------------------------
     JS saving_file
          writeFile(path,data,callback) with onkeyup()
-----------------------------------------------
     JS loading_file
        ??????????????????
 ----------------------------------------------
 
_____________________________________________

3) Project Page (LOADING FILES)
----------------------------------------------
     JS load_projects
        Checks for projects in the user's JSON obbject, Lists all the projects in user JSON (Returns list)
----------------------------------------------        
            
_________________________________________________          
 user.JSON object layout:
 -----------------------------
{name: Example,
 pass: ExamplePass,
    {project1_Example,
     project2_Example,
     project3_Example....etc}
}
     
